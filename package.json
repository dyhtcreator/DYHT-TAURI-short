{
  "name": "dwight-tauri",
  "version": "0.1.0",
  "private": true,
  "description": "Privacy-first desktop agent for audio sensing, transcription, and event logging.",
  "author": "Kelly Garnett",
  "scripts": {
    "dev": "vite --host",
    "start": "npm run tauri:dev",
    "tauri:dev": "tauri dev",
    "build": "vite build",
    "tauri:build": "tauri build",
    "preview": "vite preview",
    "install-deps": "npm install && cd src-tauri && cargo fetch",
    "clean": "rm -rf dist && rm -rf src-tauri/target",
    "setup": "npm run install-deps && npm run build"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.4",
    "@xenova/transformers": "^2.17.2",
    "better-sqlite3": "^8.7.0",
    "drizzle-kit": "^0.31.4",
    "drizzle-orm": "^0.44.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "web-audio-api": "^0.2.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.3",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
